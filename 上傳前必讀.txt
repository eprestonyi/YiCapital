Yi Capital 網站 · 最終版（FINAL）上傳說明
═══════════════════════════════════════

⚠ 拖上傳的是本資料夾「裡面的內容」，不是本資料夾本身。
⚠ 你倉庫裡已有的 CNAME 文件不在本包內——不要刪它，它管你的域名。

■ 本包已包含（無需再改任何東西）：
  · portfolios.html      已含 Yi Capital US「完整檔案頁 ↗」入口
  · fund-us.html         數據驅動組合頁（全部圖表由 Excel 即時計算）
  · assets/data/Yi_Capital_US.xlsx   ← 你的真實淨值表（130 交易日、21 持倉）
  · login.html           Admin Login / Guest Login / Guest Sign up
  · admin.html           管理後台（發布淨值表＋帳號管理，需先部署 Worker）
  · worker/worker.js     後端（Cloudflare Worker，按 worker/DEPLOY.md 部署）
  · assets/portal-config.js  部署完 Worker 後把地址填進這裡的 YC_API

■ 上傳步驟（github.dev 一次到位）：
  1. 倉庫頁按「.」鍵進入 github.dev
  2. 左側選中除 CNAME 外的全部舊文件/文件夾 → 右鍵 Delete
  3. 把本資料夾裡的全部內容拖進左側文件樹
  4. 左側源代碼管理（樹杈圖標）→ 填提交信息 → ✓ 提交並推送
  5. 回 github.com 確認所有文件時間戳為 now → 等 1 分鐘部署
  6. 無痕窗口驗收：
     www.yicapital.co/portfolios.html  （US 標題旁有入口）
     www.yicapital.co/fund-us.html     （整頁圖表為你的真實數據）
     www.yicapital.co/login.html       （登入頁，後端部署前只是界面）

■ 之後：按 worker/DEPLOY.md 部署後端（10 分鐘）→ 登入後台即可
  發布淨值表、管理訪客帳號。
